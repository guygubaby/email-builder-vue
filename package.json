{
  "devDependencies": {
    "@types/node": "^22"
  },
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "release": "pnpm -r --filter='./packages/*' run build && pnpm -r --filter='./packages/*' publish --access public --registry http://192.168.2.202:4873/"
  }
}
